local modes = {
    normal      = require("modes.normal"),
    tag         = require("modes.tag"),
    visual      = require("modes.visual"),
    launcher    = require("modes.launcher"),
    mouse       = require("modes.mouse"),
    common      = require("modes.common")
}

return modes
